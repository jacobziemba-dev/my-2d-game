export class Collision {
    constructor(game) {
        this.game = game;
    }

    resolve() {
        // Placeholder collision resolution.
        // Leave existing in-entity collisions as-is; this will be extended later.
        // Example: prevent entities from walking through walls or overlap correction.
        return;
    }
}

export default Collision;